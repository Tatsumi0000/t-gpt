[project]
name = "t-gpt"
version = "0.1.0"
description = ""
authors = [
    {name = "Tatsumi0000"}
]
readme = "README.md"
requires-python = ">=3.9,<4.0"
dependencies = [
    "llama-index (>=0.12.30,<0.13.0)",
    "llama-index-readers-confluence (>=0.3.1,<0.4.0)",
    "python-dotenv (>=1.1.0,<2.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false
